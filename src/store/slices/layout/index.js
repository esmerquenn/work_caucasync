import { createSlice } from "@reduxjs/toolkit"

const initialState = {
    IsActiveBrandDropdown: false
}

export const LayoutSlice = createSlice({
    name: "layout",
    initialState,
    reducers: {}
})